# ECF/VVUQ/whole-device integration audit

The bridge binds the dedicated engineering-control-fault request and receipt, with provider id `engineering-control-fault`, exact request/result hashes, and zero evidence credit. It is a recoverable non-bridge gap; it does not coerce the operational receipt into generic Validation/UQ evidence.

The whole-device request requires the exact ordered stage contract and enumerates missing real multi-region provider, generic VVUQ bridge, held-out physical validation, UQ artifacts, and high-fidelity closure/terminal authority. All closure, validation, evidence and promotion authority remains false.

The independent full-chain validation path revalidates the ECF registry,
dispatch request, operational receipt, nested execution identity and exact
ordered stage tuple. Negative controls reject foreign provider, request and
result hashes and an unexecuted identity.
