# DESC geometry program interpreter v4 report

The bounded candidate/context fixture covers explicit per-mode radial laws,
theta seam periodicity, one-field-period zeta rotation/equivariance for the
coordinate and every Jacobian column, metric seam invariance,
analytic-Jacobian versus central finite differences, metric symmetry, and the
exact SI scaling laws `x,J ~ L`, `g ~ L^2`, and `det(J) ~ L^3`. It also covers
canonical replay, typed multi-root `AtomicMIMO` ownership, default-registry
isolation, private construction, foreign-context rejection, recomputed-hash
forgeries, and adversarial nonfinite/domain inputs. The focused test file and
standalone runner are the authoritative execution surfaces for this isolated
edge.

Claim ceiling remains `screen_only`; `bridge_ready` or an executable program
does not constitute geometry proof, DESC provider validation, engineering
validation, or whole-device readiness.
