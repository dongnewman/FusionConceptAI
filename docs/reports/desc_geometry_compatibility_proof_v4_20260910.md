# DESC geometry compatibility proof v4 report

The positive candidate fixture produced a sealed `proved` resolution and a
strict normalized oriented-volume-density lower bound using analytic Fourier
inequalities with directed 256-bit MPFR rounding. The proof binds the exact
candidate, compiled prefix, context, physical subject, declaration, support,
chart, graph, coordinate and metric programs, root bridge, interpreter
evaluation, and geometry payload.

Focused acceptance passed 89/89 assertions with process exit code 0. Coverage
includes the exact phase and NFP conventions, boundary/tangential-derivative
identity, symbolic poloidal periodicity and field-period rotational
equivariance, `g=J'J`, axis
regularity, positive global major-radius/cross-section/volume bounds, canonical
replay, a current-prover family gap for an unsupported cubic extension,
private-constructor and recomputed-hash forgery rejection, foreign-context
rejection, directed BigFloat-to-Float64 lower-bound conversion, and the
complete authority ceiling. A supplemental grid checks the
implemented analytic bound against evaluated points, but is explicitly not
used to issue the certificate.

`geometric_compatibility_proved=true` applies only to the declared Fourier
program. `request_emitted=false`, `provider_selected=false`,
`solver_executed=false`, physical and engineering validation remain false, and
the credible-device count remains zero.

The standalone runner returned exit code 0 after explicit status, authority,
and context-bound replay validation. Adjacent regressions also returned exit
code 0: interpreter 81/81, geometry preflight 118/118, fixed-boundary request
compiler 223/223, normalized/SI bridge 90/90, forward context 72/72, core
26/26, spine 54/54, trusted registry 65/65, and FreeGS execution 42/42. The
full package test suite also completed with process exit code 0.
Independent review found no P1/P2 blocking issue. This milestone stops here;
the next DESC edge must execute an actual request/provider or record a
reproducible external-environment blocker.
