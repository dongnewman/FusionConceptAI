# DESC rho partition/trace specification report

The isolated contract binds context, candidate, compiled prefix, subject,
physical/support/chart/graph/coordinate/metric/geometry/proof, execution,
field, basis-request/result/receipt, point, and basis-sample identities from the
accepted real DESC chain. Its structural checks cover exact interval adjacency
and closure coverage from rho zero through rho one while excluding the axis
from all samples, constant-rho ordered interfaces, exact trace-to-basis sample
reconstruction, and paired opposite unit normal declarations.

The normalized-rho cover is not yet a full 3-D spatial-partition proof, and the
normal vectors are declarations rather than provider-computed geometry.
Therefore `spatial_partition_geometry_validated=false`,
`normal_geometry_validated=false`, and `interface_trace_executed=false` remain
explicit. Screening always reruns the external basis-chain validator rather
than trusting local request hash consistency. It performs no provider execution
or multi-region solve. All
convergence, closure, physical/engineering validation, evidence, pass,
promotion, P5, and terminal authority remain false; credible-device count is 0.

Verification recorded for this increment:

- isolated contract module load: `LOAD_OK` (exit 0);
- isolated source/example/test/runner parse: `PARSE_OK` for all four files
  (exit 0);
- focused test with a fresh real DESC upstream execution: five testsets,
  64/64 passed, `DESC_RHO_PARTITION_TRACE_PROCESS_EXIT=0`;
- adversarial coverage includes foreign support/context/candidate, truncated
  sample identity cover, rho gaps/overlap/axis/out-of-range, reversed or missing
  adjacency, non-opposite/non-unit normals, substituted basis fields, foreign
  sample hashes, internally self-consistent receipts whose external output is
  missing, a same-named validator injected from a foreign module, direct
  screening of a foreign request, and attempted elevation
  of every spatial/provider/execution/convergence/validation/evidence/pass/
  promotion/P5/terminal/credible-device/claim-ceiling field;
- standalone runner with a separate fresh real DESC execution emitted
  `DESC_RHO_PARTITION_TRACE_OK`,
  `DESC_RHO_PARTITION_TRACE_RUN_EXIT_CODE=0`, and wrapper process exit 0;
- separate-process related regressions passed: DESC field-basis bridge 95/95,
  3-D physical-input composition 157/157, and conservative multi-region
  manufactured control 117/117, with
  `RHO_PARTITION_RELATED_REGRESSIONS_PROCESS_EXIT=0`;
- full `julia --project=. test/runtests.jl` completed with
  `FULL_TEST_PROCESS_EXIT=0`.

Acceptance ceiling: this is a replayable structural partition and trace
sampling specification only. The next blocking edge is a real provider-derived
rho-surface normal/trace artifact followed by candidate-derived constitutive
and conservative interface-flux execution.
